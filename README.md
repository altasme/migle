# Mingle

Pixel-avatar social hangout app. See `CLAUDE.md` for the full spec, stack, and build order.

## Dev setup

```bash
npm install
cp .env.example .env   # fill in VITE_SUPABASE_URL and VITE_SUPABASE_ANON_KEY
npm run dev
```
