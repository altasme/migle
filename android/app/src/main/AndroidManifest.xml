<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode|navigation|density"
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBarLaunch"
            android:launchMode="singleTask"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!-- Catches the redirect back from an OAuth provider's consent
                 screen (opened in the system browser - Facebook has no
                 native Android sign-in path that works with Supabase the
                 way Google's Credential Manager flow does). Must also be
                 registered as a Redirect URL in the Supabase dashboard.
                 Provider-agnostic: any provider using Supabase's OAuth
                 redirect flow (Facebook now, Discord later if added) shares
                 this same callback. -->
            <intent-filter android:autoVerify="false">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="com.mingleverse.app" android:host="auth-callback" />
            </intent-filter>

        </activity>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths"></meta-data>
        </provider>
    </application>

    <!-- Permissions -->

    <uses-permission android:name="android.permission.INTERNET" />

    <!-- LiveKit voice (VibeMatch, hangout rooms) needs the mic. Capacitor's
         default WebChromeClient already forwards WebRTC's getUserMedia()
         permission prompt to these runtime permissions - nothing else to
         wire up. Feature declared optional so the app can still install
         (text-only) on a device without a microphone. -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />
    <uses-feature android:name="android.hardware.microphone" android:required="false" />

    <!-- Required on Android 13+ (targetSdk 36 here) before the app can post
         ANY system notification, including one FCM successfully delivered -
         without this declared, the OS silently drops it rather than
         prompting or erroring, which is why push notifications never
         showed up even though everything server-side was working. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
</manifest>
