// Coins/gems/gifting/CP-partner system is hidden from the UI for now (the
// owner's call, made while the product focuses on the core meet-people
// loop first). Nothing backend-side changed - wallets, gift_catalog,
// buy_cosmetic, relationships, etc. all still work exactly as before.
// Flip this back on to bring the UI back everywhere it's gated.
export const ECONOMY_ENABLED = false
